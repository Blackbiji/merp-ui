# Architecture graphique 0.5

- aucun ancien asset n’est conservé ;
- chaque famille possède son propre répertoire ;
- tous les assets sont référencés depuis un catalogue central ;
- le code ne contient aucun chemin d’asset en dur ;
- l’API reste stable lorsque les images sont remplacées.
